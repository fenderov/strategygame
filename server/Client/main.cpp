#include <QCoreApplication>
#include <QThread>
#include "client.h"

int main(int argc, char *argv[])
{
    Client client;
    QThread::sleep(2);
    QThread::sleep(2);
    QThread::sleep(2);
    QThread::sleep(2);
    QThread::sleep(2);
    QThread::sleep(2);
}
