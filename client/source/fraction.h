#ifndef FRACTION_H
#define FRACTION_H

//now no use

class Fraction
{
public:
    Fraction();
};

enum FractionType {
    FractionNull = 0,
    FractionA = 1,
    FractionB = 2
};

#endif // FRACTION_H
